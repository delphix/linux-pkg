#
# This build list includes all the packages that are build for specific
# kernel versions.
#

# Note: The following packages should be built first because other packages
# depend on them being built.
#  - zfs is required by grub2
zfs

connstat
delphix-kernel
grub2
